import { Container } from './styles'

const Developers = (dev, linkedin) => {



  return (
    <Container onClick={}>
      <img src={dev} alt="dev" />
    </Container>
  )
}

export default Developers